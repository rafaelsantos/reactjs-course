import React from 'react'
import PageTitle from '../../components/layout/PageTitle'

const UseState = (props) => {
    return (
        <div className="UseState">
            <PageTitle
                title="Hook UseState"
                subtitle="Controlling state in components"
            />
        </div>
    )
}

export default UseState
